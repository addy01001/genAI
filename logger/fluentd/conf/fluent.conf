<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match *>
  @type loki
  endpoint_url "http://loki:3100"
  labels          {"env":"prod","from":"loki"}
</match>

# <match *>
#   @type stdout
# </match>